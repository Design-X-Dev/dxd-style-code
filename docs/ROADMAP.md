# Дорожная карта компонентов DXD Style Code

## Введение

Этот документ описывает дорожную карту развития библиотеки компонентов DXD Style Code. Здесь представлены все компоненты, которые планируется добавить в библиотеку для создания полноценного набора UI-компонентов, основанного на принципах атомарного дизайна и семантической разметки.

## Текущее состояние

Библиотека уже содержит **70 компонентов**, организованных по категориям:

- **Атомы (Atoms)**: 23 компонента - базовые строительные блоки
- **Молекулы (Molecules)**: 27 компонентов - составные компоненты
- **Организмы (Organisms)**: 7 компонентов - сложные UI структуры
- **Типография (Typography)**: 6 компонентов - текстовые элементы
- **Макет (Layout)**: 7 компонентов - компоненты компоновки

Полный список существующих компонентов можно найти в [документации компонентов](./COMPONENTS.md).

## План развития

---

## 🔵 Атомы (Atoms)

### DXLink

**Приоритет**: Высокий

**Назначение**: Семантический компонент для создания ссылок с поддержкой ARIA-атрибутов и доступности.

**Сценарии использования**:

- Навигационные ссылки в меню и хлебных крошках
- Внешние ссылки с индикацией (иконка внешней ссылки)
- Ссылки в текстовом контенте
- Ссылки-кнопки (когда нужен стиль кнопки, но семантика ссылки)
- Ссылки для скачивания файлов

**Ключевые функции**:

- Рендеринг правильного HTML-тега `<a>` с семантическими атрибутами
- Поддержка `href`, `target`, `rel` атрибутов
- Автоматическое добавление `aria-label` для доступности
- Визуальная индикация внешних ссылок
- Поддержка различных вариантов стилизации (primary, secondary, ghost, link)
- Поддержка состояний (hover, active, disabled)
- Интеграция с роутингом (Vue Router)

**Интеграция**:

- Используется в `DXBreadcrumb` для навигационных элементов
- Используется в `DXSidebarMenu` для пунктов меню
- Может использоваться в любых компонентах, где нужны ссылки

**Пример использования**:

```vue
<template>
  <div>
    <!-- Базовая ссылка -->
    <DXLink href="/about">О нас</DXLink>

    <!-- Внешняя ссылка с индикацией -->
    <DXLink
      href="https://example.com"
      target="_blank"
      rel="noopener noreferrer"
      variant="primary"
    >
      Внешний сайт
    </DXLink>

    <!-- Ссылка-кнопка -->
    <DXLink href="/download" variant="button" size="md"> Скачать файл </DXLink>

    <!-- Ссылка с Vue Router -->
    <DXLink :to="{ name: 'profile', params: { id: userId } }" variant="ghost">
      Профиль пользователя
    </DXLink>

    <!-- Отключенная ссылка -->
    <DXLink href="/disabled" disabled> Недоступная ссылка </DXLink>
  </div>
</template>

<script setup>
import { ref } from "vue";
import { DXLink } from "dxd-style-code";

const userId = ref(123);
</script>
```

---

### DXQuote

**Приоритет**: Низкий

**Назначение**: Компонент для отображения inline-цитат в текстовом контенте (отдельно от блочных цитат).

**Сценарии использования**:

- Inline-цитаты в статьях и блогах
- Выделение важных фраз в тексте
- Цитаты в комментариях и обсуждениях
- Ссылки на источники в тексте

**Ключевые функции**:

- Рендеринг семантического тега `<q>` для inline-цитат
- Поддержка атрибута `cite` для указания источника
- Визуальное выделение цитаты (кавычки, стилизация)
- Поддержка различных стилей кавычек
- Интеграция с типографикой

**Интеграция**:

- Используется вместе с `DXText` и `DXBlockquote`
- Может использоваться в `DXCard` для выделения цитат в контенте

**Пример использования**:

```vue
<template>
  <div>
    <!-- Базовая inline-цитата -->
    <DXText>
      Как говорил Эйнштейн:
      <DXQuote cite="https://example.com/einstein">
        Воображение важнее знания
      </DXQuote>
    </DXText>

    <!-- Цитата в статье -->
    <DXText>
      В своей книге автор отмечает, что
      <DXQuote variant="highlighted">
        успех приходит к тем, кто действует
      </DXQuote>
      , а не к тем, кто только мечтает.
    </DXText>

    <!-- Цитата с кастомными кавычками -->
    <DXText>
      <DXQuote quote-style="french"> Французский стиль кавычек </DXQuote>
    </DXText>
  </div>
</template>

<script setup>
import { DXText, DXQuote } from "dxd-style-code";
</script>
```

---

## 🟢 Молекулы (Molecules)

### DXTimePicker

**Приоритет**: Высокий

**Назначение**: Компонент для выбора времени с поддержкой различных форматов и валидации.

**Сценарии использования**:

- Формы бронирования и планирования
- Настройка времени уведомлений
- Выбор времени начала/окончания событий
- Интеграция с календарем для выбора даты и времени
- Настройки расписания

**Ключевые функции**:

- Выбор времени через dropdown или инпут с валидацией
- Поддержка форматов (12/24 часа, с секундами)
- Валидация введенного времени
- Поддержка диапазонов времени (min/max)
- Интеграция с `DXDatePicker` для выбора даты и времени
- Поддержка часовых поясов
- Доступность (клавиатурная навигация, ARIA)

**Интеграция**:

- Используется вместе с `DXDatePicker` в формах
- Интегрируется с `DXFormControl` для валидации
- Может использоваться в `DXInputGroup` с иконками

**Пример использования**:

```vue
<template>
  <div>
    <!-- Базовый выбор времени -->
    <DXFormControl label="Время начала">
      <DXTimePicker v-model="startTime" format="24h" />
    </DXFormControl>

    <!-- Выбор времени с секундами -->
    <DXFormControl label="Точное время">
      <DXTimePicker v-model="exactTime" format="24h" :show-seconds="true" />
    </DXFormControl>

    <!-- 12-часовой формат -->
    <DXFormControl label="Время встречи">
      <DXTimePicker
        v-model="meetingTime"
        format="12h"
        placeholder="Выберите время"
      />
    </DXFormControl>

    <!-- С ограничениями -->
    <DXFormControl label="Рабочее время">
      <DXTimePicker
        v-model="workTime"
        :min-time="'09:00'"
        :max-time="'18:00'"
        format="24h"
      />
    </DXFormControl>

    <!-- Интеграция с DatePicker -->
    <DXFormControl label="Дата и время события">
      <div class="datetime-group">
        <DXDatePicker v-model="eventDate" />
        <DXTimePicker v-model="eventTime" />
      </div>
    </DXFormControl>
  </div>
</template>

<script setup>
import { ref } from "vue";
import { DXTimePicker, DXDatePicker, DXFormControl } from "dxd-style-code";

const startTime = ref("10:00");
const exactTime = ref("14:30:45");
const meetingTime = ref(null);
const workTime = ref("09:00");
const eventDate = ref(null);
const eventTime = ref(null);
</script>
```

---

### DXSearchBar

**Приоритет**: Средний

**Назначение**: Поисковая строка с автодополнением и расширенными возможностями поиска.

**Сценарии использования**:

- Глобальный поиск в приложении
- Поиск в таблицах и списках
- Поиск товаров в каталоге
- Поиск пользователей и контактов
- Поиск в документации

**Ключевые функции**:

- Поисковая строка с иконкой поиска
- Автодополнение с выпадающим списком результатов
- Поддержка горячих клавиш (Ctrl/Cmd + K)
- История поисковых запросов
- Фильтры и расширенные опции поиска
- Индикация процесса поиска
- Очистка поискового запроса
- Поддержка debounce для оптимизации запросов

**Интеграция**:

- Используется в `DXHeaderBar` для глобального поиска
- Может использоваться в `DXTableToolbar` для поиска в таблице
- Интегрируется с `DXDropdown` для результатов автодополнения

**Пример использования**:

```vue
<template>
  <div>
    <!-- Базовая поисковая строка -->
    <DXSearchBar
      v-model="searchQuery"
      placeholder="Поиск..."
      @search="handleSearch"
    />

    <!-- С автодополнением -->
    <DXSearchBar
      v-model="searchQuery"
      :suggestions="suggestions"
      :loading="isLoading"
      placeholder="Поиск товаров..."
      @search="handleSearch"
      @select-suggestion="handleSelectSuggestion"
    />

    <!-- С горячей клавишей -->
    <DXSearchBar
      v-model="globalSearch"
      hotkey="ctrl+k"
      placeholder="Глобальный поиск (Ctrl+K)"
      @search="handleGlobalSearch"
    />

    <!-- С историей поиска -->
    <DXSearchBar
      v-model="searchQuery"
      :show-history="true"
      :history-items="searchHistory"
      placeholder="Поиск с историей..."
      @search="handleSearch"
    />

    <!-- С фильтрами -->
    <DXSearchBar
      v-model="searchQuery"
      :filters="searchFilters"
      placeholder="Расширенный поиск..."
      @search="handleSearch"
      @filter-change="handleFilterChange"
    />
  </div>
</template>

<script setup>
import { ref, computed } from "vue";
import { DXSearchBar } from "dxd-style-code";

const searchQuery = ref("");
const globalSearch = ref("");
const isLoading = ref(false);
const searchHistory = ref(["Vue 3", "TypeScript", "Composition API"]);

const suggestions = computed(() => {
  if (!searchQuery.value) return [];
  return [
    { id: 1, title: "Vue 3 документация", category: "Docs" },
    { id: 2, title: "Vue Router", category: "Library" },
    { id: 3, title: "Vuex/Pinia", category: "State" },
  ].filter((item) =>
    item.title.toLowerCase().includes(searchQuery.value.toLowerCase())
  );
});

const searchFilters = ref([
  { id: "type", label: "Тип", options: ["Все", "Документы", "Код"] },
  { id: "date", label: "Дата", options: ["Все", "Сегодня", "Неделя"] },
]);

const handleSearch = (query) => {
  console.log("Поиск:", query);
  // Логика поиска
};

const handleGlobalSearch = (query) => {
  console.log("Глобальный поиск:", query);
};

const handleSelectSuggestion = (suggestion) => {
  console.log("Выбрано:", suggestion);
};

const handleFilterChange = (filters) => {
  console.log("Фильтры:", filters);
};
</script>
```

---

### DXStatCard

**Приоритет**: Средний

**Назначение**: Карточка для отображения статистики с иконкой, значением и метаданными.

**Сценарии использования**:

- Дашборды с метриками и KPI
- Карточки статистики в отчетах
- Виджеты с числовыми показателями
- Индикаторы производительности
- Сводная информация в панелях управления

**Ключевые функции**:

- Отображение основного значения (число, процент, валюта)
- Иконка или визуальный индикатор
- Заголовок и описание метрики
- Тренд (рост/падение) с визуальной индикацией
- Сравнение с предыдущим периодом
- Поддержка различных размеров
- Поддержка цветовых схем для разных типов метрик
- Кликабельность для перехода к деталям

**Интеграция**:

- Используется в `DXDashboardGrid` для создания дашбордов
- Может использоваться в `DXCard` как контент
- Интегрируется с `DXIcon` для визуальных индикаторов

**Пример использования**:

```vue
<template>
  <div class="dashboard">
    <!-- Базовая карточка статистики -->
    <DXStatCard title="Всего пользователей" :value="1250" icon="users" />

    <!-- С трендом -->
    <DXStatCard
      title="Продажи"
      :value="45230"
      value-format="currency"
      currency="USD"
      :trend="{ value: 12.5, direction: 'up' }"
      icon="chart-line"
      :comparison="'vs прошлый месяц'"
    />

    <!-- С процентом -->
    <DXStatCard
      title="Конверсия"
      :value="24.5"
      value-format="percentage"
      :trend="{ value: 3.2, direction: 'up' }"
      icon="percent"
      color="success"
    />

    <!-- Кликабельная карточка -->
    <DXStatCard
      title="Новые заказы"
      :value="89"
      icon="shopping-cart"
      color="primary"
      clickable
      @click="handleCardClick"
    />

    <!-- С описанием -->
    <DXStatCard
      title="Активные проекты"
      :value="15"
      description="Из 20 запланированных"
      icon="folder"
      size="lg"
    />
  </div>
</template>

<script setup>
import { DXStatCard } from "dxd-style-code";

const handleCardClick = () => {
  console.log("Карточка кликнута");
};
</script>

<style scoped>
.dashboard {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 1rem;
}
</style>
```

---

### DXRating

**Приоритет**: Средний

**Назначение**: Компонент для отображения и выбора рейтинга с использованием звезд или других символов.

**Сценарии использования**:

- Оценка товаров и услуг
- Рейтинги в отзывах
- Оценка качества контента
- Системы обратной связи
- Рейтинги в каталогах

**Ключевые функции**:

- Отображение рейтинга (read-only режим)
- Выбор рейтинга (интерактивный режим)
- Поддержка различных символов (звезды, сердца, числа)
- Поддержка дробных значений (половина звезды)
- Поддержка различных размеров
- Поддержка цветовых схем
- Отображение количества оценок
- Доступность (клавиатурная навигация, ARIA)

**Интеграция**:

- Используется в формах отзывов
- Может использоваться в `DXCard` для отображения рейтинга товара
- Интегрируется с `DXFormControl` для валидации

**Пример использования**:

```vue
<template>
  <div>
    <!-- Только отображение рейтинга -->
    <DXRating :value="4.5" :max="5" readonly />

    <!-- Интерактивный выбор рейтинга -->
    <DXFormControl label="Оцените товар">
      <DXRating v-model="productRating" :max="5" @change="handleRatingChange" />
    </DXFormControl>

    <!-- С дробными значениями -->
    <DXRating :value="3.5" :max="5" :allow-half="true" readonly />

    <!-- С количеством оценок -->
    <DXRating :value="4.2" :max="5" :count="125" readonly />

    <!-- Кастомные символы (сердца) -->
    <DXRating v-model="heartRating" :max="5" symbol="heart" color="red" />

    <!-- Различные размеры -->
    <DXRating :value="4" size="sm" readonly />
    <DXRating :value="4" size="md" readonly />
    <DXRating :value="4" size="lg" readonly />
  </div>
</template>

<script setup>
import { ref } from "vue";
import { DXRating, DXFormControl } from "dxd-style-code";

const productRating = ref(0);
const heartRating = ref(0);

const handleRatingChange = (value) => {
  console.log("Выбран рейтинг:", value);
};
</script>
```

---

## 🟡 Организмы (Organisms)

### DXAppLayout

**Приоритет**: Высокий

**Назначение**: Основной макет приложения, объединяющий хедер, сайдбар, контент и футер.

**Сценарии использования**:

- Базовый макет для всех страниц приложения
- Адаптивный макет с коллапсирующим сайдбаром
- Макет с различными конфигурациями (с сайдбаром/без, с футером/без)
- Макет для админ-панелей и дашбордов

**Ключевые функции**:

- Семантическая структура (header, aside, main, footer)
- Адаптивное поведение (скрытие сайдбара на мобильных)
- Управление состоянием сайдбара (открыт/закрыт)
- Поддержка различных режимов сайдбара (overlay, push, static)
- Зоны для контента через slots
- Интеграция с роутингом
- Поддержка различных тем оформления

**Интеграция**:

- Использует `DXHeaderBar` для верхней панели
- Использует `DXSidebarMenu` для боковой навигации
- Использует `DXContainer` для контентной области
- Может использовать `DXFooter` (если будет создан)

**Пример использования**:

```vue
<template>
  <DXAppLayout
    :sidebar-collapsed="isSidebarCollapsed"
    sidebar-mode="overlay"
    @sidebar-toggle="handleSidebarToggle"
  >
    <template #header>
      <DXHeaderBar :logo="logoUrl" app-name="My App">
        <template #navigation>
          <DXLink href="/dashboard">Дашборд</DXLink>
          <DXLink href="/users">Пользователи</DXLink>
        </template>
        <template #actions>
          <DXButton icon="bell" variant="ghost" />
          <DXButton icon="user" variant="ghost" />
        </template>
      </DXHeaderBar>
    </template>

    <template #sidebar>
      <DXSidebarMenu :items="menuItems" />
    </template>

    <template #content>
      <DXContainer>
        <h1>Основной контент</h1>
        <!-- Контент страницы -->
      </DXContainer>
    </template>

    <template #footer>
      <footer>© 2024 My App</footer>
    </template>
  </DXAppLayout>
</template>

<script setup>
import { ref } from "vue";
import {
  DXAppLayout,
  DXHeaderBar,
  DXSidebarMenu,
  DXContainer,
  DXLink,
  DXButton,
} from "dxd-style-code";

const isSidebarCollapsed = ref(false);
const logoUrl = ref("/logo.png");

const menuItems = ref([
  { id: 1, label: "Главная", href: "/", icon: "home" },
  { id: 2, label: "Проекты", href: "/projects", icon: "folder" },
  { id: 3, label: "Настройки", href: "/settings", icon: "cog" },
]);

const handleSidebarToggle = (collapsed) => {
  isSidebarCollapsed.value = collapsed;
};
</script>
```

---

### DXHeaderBar

**Приоритет**: Высокий

**Назначение**: Верхняя панель приложения с навигацией, поиском, уведомлениями и профилем пользователя.

**Сценарии использования**:

- Главная навигационная панель приложения
- Панель с глобальным поиском
- Панель с уведомлениями и профилем
- Адаптивная панель для мобильных устройств

**Ключевые функции**:

- Логотип и название приложения
- Основная навигация (меню, вкладки)
- Глобальный поиск (интеграция с `DXSearchBar`)
- Иконки действий (уведомления, настройки)
- Профиль пользователя с dropdown меню
- Адаптивное меню для мобильных (гамбургер-меню)
- Sticky/fixed позиционирование
- Поддержка различных высот и стилей

**Интеграция**:

- Используется в `DXAppLayout` как верхняя панель
- Использует `DXSearchBar` для поиска
- Использует `DXDropdown` для меню профиля
- Использует `DXButton`, `DXIcon` для элементов интерфейса

**Пример использования**:

```vue
<template>
  <DXHeaderBar :logo="logoUrl" app-name="My Application" :sticky="true">
    <template #navigation>
      <DXLink href="/dashboard">Дашборд</DXLink>
      <DXLink href="/projects">Проекты</DXLink>
      <DXLink href="/analytics">Аналитика</DXLink>
    </template>

    <template #search>
      <DXSearchBar
        v-model="searchQuery"
        placeholder="Поиск..."
        @search="handleSearch"
      />
    </template>

    <template #actions>
      <DXButton icon="bell" variant="ghost" :badge="unreadCount" />
      <DXButton icon="settings" variant="ghost" />
      <DXDropdown :items="userMenuItems" trigger="click">
        <template #trigger>
          <DXAvatar :src="userAvatar" :name="userName" />
        </template>
      </DXDropdown>
    </template>

    <template #mobile-menu>
      <DXButton icon="menu" variant="ghost" @click="toggleMobileMenu" />
    </template>
  </DXHeaderBar>
</template>

<script setup>
import { ref } from "vue";
import {
  DXHeaderBar,
  DXSearchBar,
  DXLink,
  DXButton,
  DXDropdown,
  DXAvatar,
} from "dxd-style-code";

const logoUrl = ref("/logo.png");
const searchQuery = ref("");
const unreadCount = ref(5);
const userAvatar = ref("/avatar.jpg");
const userName = ref("John Doe");

const userMenuItems = ref([
  { id: 1, label: "Профиль", href: "/profile" },
  { id: 2, label: "Настройки", href: "/settings" },
  { id: 3, label: "Выйти", action: "logout" },
]);

const handleSearch = (query) => {
  console.log("Поиск:", query);
};

const toggleMobileMenu = () => {
  // Логика мобильного меню
};
</script>
```

---

### DXSidebar

**Приоритет**: Средний

**Назначение**: Обертка для боковой панели с поддержкой различных режимов отображения и анимаций.

**Сценарии использования**:

- Боковая панель навигации в приложении
- Панель фильтров в каталогах
- Панель настроек
- Контекстная боковая панель

**Ключевые функции**:

- Различные режимы отображения (overlay, push, static)
- Анимации открытия/закрытия
- Управление шириной панели
- Поддержка коллапсирования (мини-версия)
- Адаптивное поведение (скрытие на мобильных)
- Backdrop для overlay режима
- Зоны для контента через slots

**Интеграция**:

- Используется в `DXAppLayout` для боковой навигации
- Использует `DXSidebarMenu` для меню
- Использует `DXBackdrop` для overlay режима

**Пример использования**:

```vue
<template>
  <DXSidebar
    v-model:open="isOpen"
    mode="overlay"
    :width="280"
    :collapsed-width="64"
    :collapsed="isCollapsed"
    @toggle="handleToggle"
  >
    <template #header>
      <div class="sidebar-header">
        <DXIcon name="logo" size="lg" />
        <span v-if="!isCollapsed">My App</span>
      </div>
    </template>

    <template #content>
      <DXSidebarMenu :items="menuItems" :collapsed="isCollapsed" />
    </template>

    <template #footer>
      <DXButton
        :icon="isCollapsed ? 'chevron-right' : 'chevron-left'"
        variant="ghost"
        @click="toggleCollapse"
      />
    </template>
  </DXSidebar>
</template>

<script setup>
import { ref } from "vue";
import { DXSidebar, DXSidebarMenu, DXButton, DXIcon } from "dxd-style-code";

const isOpen = ref(true);
const isCollapsed = ref(false);

const menuItems = ref([
  { id: 1, label: "Главная", href: "/", icon: "home" },
  { id: 2, label: "Проекты", href: "/projects", icon: "folder" },
  { id: 3, label: "Настройки", href: "/settings", icon: "cog" },
]);

const handleToggle = (open) => {
  isOpen.value = open;
};

const toggleCollapse = () => {
  isCollapsed.value = !isCollapsed.value;
};
</script>
```

---

### DXDashboardGrid

**Приоритет**: Средний

**Назначение**: Сетка для дашборда с поддержкой drag-and-drop для перестановки виджетов.

**Сценарии использования**:

- Настраиваемые дашборды
- Панели мониторинга
- Аналитические панели
- Персонализируемые рабочие пространства

**Ключевые функции**:

- Адаптивная сетка с колонками
- Drag-and-drop для перестановки виджетов
- Изменение размера виджетов (resize)
- Сохранение конфигурации дашборда
- Поддержка различных размеров виджетов
- Добавление/удаление виджетов
- Настройка виджетов
- Адаптивность для мобильных устройств

**Интеграция**:

- Использует `DXDashboardWidget` для виджетов
- Использует `DXStatCard` для карточек статистики
- Может использовать `DXChartContainer` для графиков
- Использует `DXGrid` для базовой сетки

**Пример использования**:

```vue
<template>
  <DXDashboardGrid
    v-model:widgets="widgets"
    :columns="12"
    :row-height="100"
    @widget-move="handleWidgetMove"
    @widget-resize="handleWidgetResize"
  >
    <template #widget="{ widget }">
      <DXDashboardWidget
        :id="widget.id"
        :type="widget.type"
        :title="widget.title"
        :config="widget.config"
        @edit="handleWidgetEdit"
        @delete="handleWidgetDelete"
      >
        <template v-if="widget.type === 'stat'">
          <DXStatCard
            :title="widget.config.title"
            :value="widget.config.value"
            :icon="widget.config.icon"
          />
        </template>
        <template v-else-if="widget.type === 'chart'">
          <DXChartContainer
            :type="widget.config.chartType"
            :data="widget.config.data"
          />
        </template>
      </DXDashboardWidget>
    </template>
  </DXDashboardGrid>
</template>

<script setup>
import { ref } from "vue";
import {
  DXDashboardGrid,
  DXDashboardWidget,
  DXStatCard,
  DXChartContainer,
} from "dxd-style-code";

const widgets = ref([
  {
    id: 1,
    x: 0,
    y: 0,
    w: 4,
    h: 2,
    type: "stat",
    title: "Пользователи",
    config: { title: "Всего", value: 1250, icon: "users" },
  },
  {
    id: 2,
    x: 4,
    y: 0,
    w: 4,
    h: 2,
    type: "stat",
    title: "Продажи",
    config: { title: "Сегодня", value: 45230, icon: "chart-line" },
  },
  {
    id: 3,
    x: 8,
    y: 0,
    w: 4,
    h: 4,
    type: "chart",
    title: "График продаж",
    config: {
      chartType: "line",
      data: [
        /* данные графика */
      ],
    },
  },
]);

const handleWidgetMove = (widgetId, newPosition) => {
  console.log("Виджет перемещен:", widgetId, newPosition);
};

const handleWidgetResize = (widgetId, newSize) => {
  console.log("Виджет изменен:", widgetId, newSize);
};

const handleWidgetEdit = (widgetId) => {
  console.log("Редактирование виджета:", widgetId);
};

const handleWidgetDelete = (widgetId) => {
  widgets.value = widgets.value.filter((w) => w.id !== widgetId);
};
</script>
```

---

### DXFormWizard

**Приоритет**: Средний

**Назначение**: Многошаговая форма с индикатором прогресса и валидацией на каждом шаге.

**Сценарии использования**:

- Регистрация пользователей
- Настройка профиля
- Процесс оформления заказа
- Мастер настройки приложения
- Многоэтапные процессы

**Ключевые функции**:

- Навигация между шагами (вперед/назад)
- Индикатор прогресса
- Валидация на каждом шаге
- Сохранение данных между шагами
- Возможность пропуска шагов (если разрешено)
- Подтверждение перед выходом
- Поддержка различных стилей навигации (tabs, steps, dots)
- Анимации переходов между шагами

**Интеграция**:

- Использует `DXFormControl` для полей формы
- Использует `DXButton` для навигации
- Использует `DXProgress` для индикатора
- Может использовать `DXTabs` для визуализации шагов

**Пример использования**:

```vue
<template>
  <DXFormWizard
    v-model:current-step="currentStep"
    :steps="steps"
    :form-data="formData"
    @step-change="handleStepChange"
    @submit="handleSubmit"
  >
    <template #step-1>
      <DXFormControl label="Имя">
        <DXInput v-model="formData.name" required />
      </DXFormControl>
      <DXFormControl label="Email">
        <DXInput v-model="formData.email" type="email" required />
      </DXFormControl>
    </template>

    <template #step-2>
      <DXFormControl label="Компания">
        <DXInput v-model="formData.company" />
      </DXFormControl>
      <DXFormControl label="Должность">
        <DXSelect v-model="formData.position" :options="positions" />
      </DXFormControl>
    </template>

    <template #step-3>
      <DXFormControl label="О себе">
        <DXTextarea v-model="formData.bio" rows="5" />
      </DXFormControl>
    </template>

    <template #navigation="{ canGoNext, canGoPrev, goNext, goPrev }">
      <div class="wizard-navigation">
        <DXButton v-if="canGoPrev" @click="goPrev"> Назад </DXButton>
        <DXButton v-if="canGoNext" variant="primary" @click="goNext">
          Далее
        </DXButton>
        <DXButton v-else variant="primary" @click="handleSubmit">
          Завершить
        </DXButton>
      </div>
    </template>
  </DXFormWizard>
</template>

<script setup>
import { ref } from "vue";
import {
  DXFormWizard,
  DXFormControl,
  DXInput,
  DXSelect,
  DXTextarea,
  DXButton,
} from "dxd-style-code";

const currentStep = ref(0);

const steps = ref([
  { id: 1, title: "Личная информация", description: "Основные данные" },
  { id: 2, title: "Работа", description: "Профессиональная информация" },
  { id: 3, title: "Дополнительно", description: "Дополнительные сведения" },
]);

const formData = ref({
  name: "",
  email: "",
  company: "",
  position: "",
  bio: "",
});

const positions = ref([
  { value: "developer", label: "Разработчик" },
  { value: "designer", label: "Дизайнер" },
  { value: "manager", label: "Менеджер" },
]);

const handleStepChange = (step) => {
  console.log("Шаг изменен:", step);
};

const handleSubmit = () => {
  console.log("Форма отправлена:", formData.value);
};
</script>
```

---

### DXDataTable

**Приоритет**: Средний

**Назначение**: Расширенная таблица с редактированием ячеек, inline-редактированием и расширенными возможностями.

**Сценарии использования**:

- Административные панели с редактируемыми данными
- Таблицы с массовым редактированием
- Таблицы с inline-редактированием
- Таблицы с расширенными возможностями форматирования

**Ключевые функции**:

- Все функции базовой `DXTable`
- Редактирование ячеек (inline editing)
- Валидация при редактировании
- Массовое редактирование
- Отмена изменений
- Сохранение изменений
- Поддержка различных типов редакторов (текст, число, дата, select)
- История изменений

**Интеграция**:

- Расширяет функциональность `DXTable`
- Использует `DXInput`, `DXSelect`, `DXDatePicker` для редактирования
- Использует `DXFormControl` для валидации

**Пример использования**:

```vue
<template>
  <DXDataTable
    :columns="columns"
    :data="tableData"
    :editable="true"
    @cell-edit="handleCellEdit"
    @row-save="handleRowSave"
    @row-cancel="handleRowCancel"
  >
    <template #cell-actions="{ row, isEditing }">
      <div v-if="isEditing">
        <DXButton size="sm" variant="primary" @click="saveRow(row)">
          Сохранить
        </DXButton>
        <DXButton size="sm" @click="cancelEdit(row)"> Отмена </DXButton>
      </div>
      <div v-else>
        <DXButton size="sm" @click="editRow(row)"> Редактировать </DXButton>
      </div>
    </template>
  </DXDataTable>
</template>

<script setup>
import { ref } from "vue";
import { DXDataTable, DXButton } from "dxd-style-code";

const columns = ref([
  { key: "id", label: "ID", editable: false },
  { key: "name", label: "Имя", editable: true, editor: "input" },
  {
    key: "email",
    label: "Email",
    editable: true,
    editor: "input",
    type: "email",
  },
  {
    key: "role",
    label: "Роль",
    editable: true,
    editor: "select",
    options: [
      { value: "admin", label: "Администратор" },
      { value: "user", label: "Пользователь" },
    ],
  },
  { key: "created", label: "Создан", editable: true, editor: "date" },
]);

const tableData = ref([
  {
    id: 1,
    name: "John Doe",
    email: "john@example.com",
    role: "admin",
    created: "2024-01-15",
  },
  {
    id: 2,
    name: "Jane Smith",
    email: "jane@example.com",
    role: "user",
    created: "2024-01-20",
  },
]);

const handleCellEdit = (row, column, value) => {
  console.log("Ячейка отредактирована:", row, column, value);
};

const handleRowSave = (row) => {
  console.log("Строка сохранена:", row);
};

const handleRowCancel = (row) => {
  console.log("Редактирование отменено:", row);
};

const editRow = (row) => {
  // Логика начала редактирования
};

const saveRow = (row) => {
  // Логика сохранения
};

const cancelEdit = (row) => {
  // Логика отмены
};
</script>
```

---

### DXChartContainer

**Приоритет**: Низкий

**Назначение**: Контейнер для графиков с легендой, контролами и настройками отображения.

**Сценарии использования**:

- Дашборды с аналитикой
- Отчеты с графиками
- Визуализация данных
- Интерактивные графики

**Ключевые функции**:

- Контейнер для различных типов графиков (линейные, столбчатые, круговые)
- Легенда с возможностью скрытия/показа серий
- Контролы для настройки (период, группировка)
- Экспорт графиков (PNG, SVG)
- Масштабирование и панорамирование
- Tooltip при наведении
- Адаптивность для мобильных устройств
- Поддержка различных библиотек графиков

**Интеграция**:

- Используется в `DXDashboardWidget` для виджетов с графиками
- Может использоваться в `DXCard` для отображения графиков
- Интегрируется с `DXButton` для контролов

**Пример использования**:

```vue
<template>
  <DXChartContainer
    :type="chartType"
    :data="chartData"
    :options="chartOptions"
    :legend="true"
    :controls="true"
    @export="handleExport"
  >
    <template #controls>
      <DXSelect v-model="chartType" :options="chartTypes" />
      <DXSelect v-model="period" :options="periods" />
      <DXButton icon="download" @click="handleExport"> Экспорт </DXButton>
    </template>
  </DXChartContainer>
</template>

<script setup>
import { ref } from "vue";
import { DXChartContainer, DXSelect, DXButton } from "dxd-style-code";

const chartType = ref("line");
const period = ref("month");

const chartTypes = ref([
  { value: "line", label: "Линейный" },
  { value: "bar", label: "Столбчатый" },
  { value: "pie", label: "Круговой" },
]);

const periods = ref([
  { value: "week", label: "Неделя" },
  { value: "month", label: "Месяц" },
  { value: "year", label: "Год" },
]);

const chartData = ref({
  labels: ["Янв", "Фев", "Мар", "Апр"],
  datasets: [
    { label: "Продажи", data: [100, 150, 200, 180] },
    { label: "Затраты", data: [80, 120, 150, 140] },
  ],
});

const chartOptions = ref({
  responsive: true,
  maintainAspectRatio: false,
});

const handleExport = (format) => {
  console.log("Экспорт графика:", format);
};
</script>
```

---

### DXMediaGallery

**Приоритет**: Средний

**Назначение**: Галерея изображений или видео с превью, полноэкранным просмотром и навигацией.

**Сценарии использования**:

- Галереи товаров в интернет-магазинах
- Портфолио фотографий
- Галереи документов с превью
- Медиа-библиотеки
- Просмотр изображений в модальном окне

**Ключевые функции**:

- Сетка или список превью изображений
- Полноэкранный просмотр с навигацией
- Lightbox режим
- Поддержка различных форматов (изображения, видео)
- Ленивая загрузка изображений
- Zoom и панорамирование
- Поддержка жестов на мобильных
- Информация об изображении (метаданные)
- Возможность удаления/редактирования

**Интеграция**:

- Использует `DXModal` для полноэкранного просмотра
- Использует `DXButton` для навигации
- Использует `DXIcon` для иконок действий
- Может использоваться в `DXCard` для отображения галереи

**Пример использования**:

```vue
<template>
  <DXMediaGallery
    :items="galleryItems"
    :columns="3"
    layout="grid"
    :lightbox="true"
    @item-click="handleItemClick"
  >
    <template #item="{ item, index }">
      <div class="gallery-item">
        <img :src="item.thumbnail" :alt="item.title" />
        <div class="overlay">
          <DXButton icon="eye" @click="openLightbox(index)" />
          <DXButton icon="download" @click="downloadItem(item)" />
        </div>
      </div>
    </template>
  </DXMediaGallery>
</template>

<script setup>
import { ref } from "vue";
import { DXMediaGallery, DXButton } from "dxd-style-code";

const galleryItems = ref([
  {
    id: 1,
    title: "Изображение 1",
    src: "/images/image1.jpg",
    thumbnail: "/images/thumb1.jpg",
    type: "image",
  },
  {
    id: 2,
    title: "Изображение 2",
    src: "/images/image2.jpg",
    thumbnail: "/images/thumb2.jpg",
    type: "image",
  },
]);

const handleItemClick = (item, index) => {
  console.log("Клик по элементу:", item, index);
};

const openLightbox = (index) => {
  // Открытие lightbox
};

const downloadItem = (item) => {
  console.log("Скачивание:", item);
};
</script>
```

---

### DXCommentSection

**Приоритет**: Низкий

**Назначение**: Секция комментариев с поддержкой вложенности, ответов и модерации.

**Сценарии использования**:

- Комментарии к статьям и постам
- Обсуждения в блогах
- Отзывы с ответами
- Системы обратной связи

**Ключевые функции**:

- Отображение списка комментариев
- Вложенные комментарии (threading)
- Ответы на комментарии
- Редактирование и удаление комментариев
- Модерация комментариев
- Пагинация комментариев
- Фильтрация и сортировка
- Форма добавления комментария
- Поддержка реакций (лайки, дизлайки)

**Интеграция**:

- Использует `DXTextarea` для ввода комментария
- Использует `DXButton` для действий
- Использует `DXAvatar` для аватаров пользователей
- Использует `DXPagination` для пагинации

**Пример использования**:

```vue
<template>
  <DXCommentSection
    :comments="comments"
    :threading="true"
    @add-comment="handleAddComment"
    @reply="handleReply"
    @edit="handleEdit"
    @delete="handleDelete"
  >
    <template #comment-form>
      <DXTextarea
        v-model="newComment"
        placeholder="Добавить комментарий..."
        rows="3"
      />
      <DXButton variant="primary" @click="submitComment"> Отправить </DXButton>
    </template>

    <template #comment="{ comment }">
      <div class="comment">
        <DXAvatar :src="comment.author.avatar" :name="comment.author.name" />
        <div class="comment-content">
          <div class="comment-header">
            <strong>{{ comment.author.name }}</strong>
            <span class="date">{{ formatDate(comment.date) }}</span>
          </div>
          <p>{{ comment.text }}</p>
          <div class="comment-actions">
            <DXButton
              size="sm"
              variant="ghost"
              @click="likeComment(comment.id)"
            >
              👍 {{ comment.likes }}
            </DXButton>
            <DXButton
              size="sm"
              variant="ghost"
              @click="replyToComment(comment.id)"
            >
              Ответить
            </DXButton>
          </div>
        </div>
      </div>
    </template>
  </DXCommentSection>
</template>

<script setup>
import { ref } from "vue";
import {
  DXCommentSection,
  DXTextarea,
  DXButton,
  DXAvatar,
} from "dxd-style-code";

const newComment = ref("");

const comments = ref([
  {
    id: 1,
    text: "Отличная статья!",
    author: { name: "John Doe", avatar: "/avatar1.jpg" },
    date: new Date(),
    likes: 5,
    replies: [],
  },
]);

const handleAddComment = (text) => {
  console.log("Добавлен комментарий:", text);
};

const handleReply = (commentId, text) => {
  console.log("Ответ на комментарий:", commentId, text);
};

const handleEdit = (commentId, text) => {
  console.log("Редактирование:", commentId, text);
};

const handleDelete = (commentId) => {
  console.log("Удаление:", commentId);
};

const likeComment = (commentId) => {
  // Логика лайка
};

const replyToComment = (commentId) => {
  // Логика ответа
};

const submitComment = () => {
  if (newComment.value.trim()) {
    handleAddComment(newComment.value);
    newComment.value = "";
  }
};

const formatDate = (date) => {
  return new Intl.DateTimeFormat("ru-RU").format(date);
};
</script>
```

---

### DXNotificationCenter

**Приоритет**: Низкий

**Назначение**: Центр уведомлений с фильтрацией, группировкой и управлением уведомлениями.

**Сценарии использования**:

- Центр уведомлений в приложении
- Панель уведомлений в хедере
- История уведомлений
- Управление настройками уведомлений

**Ключевые функции**:

- Список уведомлений с группировкой
- Фильтрация по типам уведомлений
- Отметка как прочитанное/непрочитанное
- Удаление уведомлений
- Массовые действия
- Настройки уведомлений
- Поддержка различных типов уведомлений
- Индикация непрочитанных
- Автоматическое обновление

**Интеграция**:

- Используется в `DXHeaderBar` для иконки уведомлений
- Использует `DXDropdown` для панели уведомлений
- Использует `DXBadge` для индикации количества
- Использует `DXButton` для действий

**Пример использования**:

```vue
<template>
  <DXNotificationCenter
    :notifications="notifications"
    :unread-count="unreadCount"
    @mark-read="handleMarkRead"
    @mark-all-read="handleMarkAllRead"
    @delete="handleDelete"
    @filter="handleFilter"
  >
    <template #trigger>
      <DXButton icon="bell" variant="ghost">
        <DXBadge :count="unreadCount" />
      </DXButton>
    </template>

    <template #notification="{ notification }">
      <div
        class="notification-item"
        :class="{ unread: !notification.read }"
        @click="handleNotificationClick(notification)"
      >
        <DXIcon :name="notification.icon" />
        <div class="notification-content">
          <strong>{{ notification.title }}</strong>
          <p>{{ notification.message }}</p>
          <span class="time">{{ formatTime(notification.date) }}</span>
        </div>
        <DXButton
          size="sm"
          variant="ghost"
          icon="close"
          @click.stop="handleDelete(notification.id)"
        />
      </div>
    </template>
  </DXNotificationCenter>
</template>

<script setup>
import { ref, computed } from "vue";
import {
  DXNotificationCenter,
  DXButton,
  DXBadge,
  DXIcon,
} from "dxd-style-code";

const notifications = ref([
  {
    id: 1,
    type: "info",
    title: "Новое сообщение",
    message: "У вас новое сообщение от John Doe",
    date: new Date(),
    read: false,
    icon: "message",
  },
  {
    id: 2,
    type: "success",
    title: "Задача выполнена",
    message: 'Задача "Обновить документацию" выполнена',
    date: new Date(Date.now() - 3600000),
    read: false,
    icon: "check",
  },
]);

const unreadCount = computed(
  () => notifications.value.filter((n) => !n.read).length
);

const handleMarkRead = (id) => {
  const notification = notifications.value.find((n) => n.id === id);
  if (notification) notification.read = true;
};

const handleMarkAllRead = () => {
  notifications.value.forEach((n) => (n.read = true));
};

const handleDelete = (id) => {
  notifications.value = notifications.value.filter((n) => n.id !== id);
};

const handleFilter = (filter) => {
  console.log("Фильтр:", filter);
};

const handleNotificationClick = (notification) => {
  if (!notification.read) {
    handleMarkRead(notification.id);
  }
  // Переход к уведомлению
};

const formatTime = (date) => {
  return new Intl.RelativeTimeFormat("ru").format(
    Math.round((date - new Date()) / 60000),
    "minute"
  );
};
</script>
```

---

### DXChatInterface

**Приоритет**: Низкий

**Назначение**: Интерфейс чата с сообщениями, вводом текста и поддержкой различных типов сообщений.

**Сценарии использования**:

- Чат-интерфейсы в приложениях
- Системы поддержки
- Мессенджеры
- Командные чаты

**Ключевые функции**:

- Отображение списка сообщений
- Ввод и отправка сообщений
- Поддержка различных типов сообщений (текст, изображения, файлы)
- Индикация статуса сообщений (отправлено, доставлено, прочитано)
- Поиск по сообщениям
- Группировка сообщений по времени
- Поддержка эмодзи и форматирования
- Уведомления о новых сообщениях
- Скролл к новым сообщениям

**Интеграция**:

- Использует `DXTextarea` для ввода сообщений
- Использует `DXButton` для отправки
- Использует `DXAvatar` для аватаров
- Использует `DXFileUpload` для загрузки файлов

**Пример использования**:

```vue
<template>
  <DXChatInterface
    :messages="messages"
    :current-user="currentUser"
    @send-message="handleSendMessage"
    @upload-file="handleUploadFile"
  >
    <template #message="{ message }">
      <div
        class="message"
        :class="{ 'own-message': message.userId === currentUser.id }"
      >
        <DXAvatar
          :src="message.user.avatar"
          :name="message.user.name"
          size="sm"
        />
        <div class="message-content">
          <div class="message-header">
            <strong>{{ message.user.name }}</strong>
            <span class="time">{{ formatTime(message.date) }}</span>
          </div>
          <p>{{ message.text }}</p>
          <div v-if="message.status" class="message-status">
            {{ message.status }}
          </div>
        </div>
      </div>
    </template>

    <template #input>
      <div class="chat-input">
        <DXFileUpload
          @file-selected="handleFileSelect"
          accept="image/*,video/*"
        />
        <DXTextarea
          v-model="messageText"
          placeholder="Введите сообщение..."
          rows="2"
          @keydown.enter.exact="handleSend"
        />
        <DXButton icon="send" variant="primary" @click="handleSend">
          Отправить
        </DXButton>
      </div>
    </template>
  </DXChatInterface>
</template>

<script setup>
import { ref } from "vue";
import {
  DXChatInterface,
  DXTextarea,
  DXButton,
  DXAvatar,
  DXFileUpload,
} from "dxd-style-code";

const messageText = ref("");

const currentUser = ref({
  id: 1,
  name: "Вы",
  avatar: "/my-avatar.jpg",
});

const messages = ref([
  {
    id: 1,
    userId: 2,
    user: { name: "John Doe", avatar: "/avatar1.jpg" },
    text: "Привет! Как дела?",
    date: new Date(Date.now() - 3600000),
    status: "прочитано",
  },
  {
    id: 2,
    userId: 1,
    user: currentUser.value,
    text: "Отлично, спасибо!",
    date: new Date(Date.now() - 1800000),
    status: "доставлено",
  },
]);

const handleSendMessage = (text) => {
  messages.value.push({
    id: Date.now(),
    userId: currentUser.value.id,
    user: currentUser.value,
    text,
    date: new Date(),
    status: "отправляется",
  });
  messageText.value = "";
};

const handleSend = () => {
  if (messageText.value.trim()) {
    handleSendMessage(messageText.value);
  }
};

const handleUploadFile = (file) => {
  console.log("Загрузка файла:", file);
};

const handleFileSelect = (file) => {
  handleUploadFile(file);
};

const formatTime = (date) => {
  return new Intl.DateTimeFormat("ru-RU", {
    hour: "2-digit",
    minute: "2-digit",
  }).format(date);
};
</script>
```

---

### DXAuthenticationForm

**Приоритет**: Низкий

**Назначение**: Готовая форма аутентификации (логин/регистрация) с валидацией и обработкой ошибок.

**Сценарии использования**:

- Страницы входа в приложение
- Регистрация новых пользователей
- Восстановление пароля
- Двухфакторная аутентификация

**Ключевые функции**:

- Форма входа с email/username и паролем
- Форма регистрации с валидацией
- Восстановление пароля
- Переключение между режимами (логин/регистрация)
- Валидация полей
- Обработка ошибок аутентификации
- Интеграция с социальными сетями (OAuth)
- Двухфакторная аутентификация
- Запомнить меня

**Интеграция**:

- Использует `DXInput`, `DXPasswordInput` для полей
- Использует `DXFormControl` для валидации
- Использует `DXButton` для отправки формы
- Использует `DXAlert` для отображения ошибок

**Пример использования**:

```vue
<template>
  <DXAuthenticationForm
    :mode="authMode"
    @submit="handleSubmit"
    @mode-change="handleModeChange"
  >
    <template #login>
      <DXFormControl label="Email">
        <DXInput v-model="loginForm.email" type="email" required />
      </DXFormControl>
      <DXFormControl label="Пароль">
        <DXPasswordInput v-model="loginForm.password" required />
      </DXFormControl>
      <DXCheckbox v-model="loginForm.remember"> Запомнить меня </DXCheckbox>
      <DXButton type="submit" variant="primary" :loading="isLoading" block>
        Войти
      </DXButton>
      <DXLink href="/forgot-password"> Забыли пароль? </DXLink>
    </template>

    <template #register>
      <DXFormControl label="Имя">
        <DXInput v-model="registerForm.name" required />
      </DXFormControl>
      <DXFormControl label="Email">
        <DXInput v-model="registerForm.email" type="email" required />
      </DXFormControl>
      <DXFormControl label="Пароль">
        <DXPasswordInput v-model="registerForm.password" required />
      </DXFormControl>
      <DXFormControl label="Подтверждение пароля">
        <DXPasswordInput v-model="registerForm.confirmPassword" required />
      </DXFormControl>
      <DXButton type="submit" variant="primary" :loading="isLoading" block>
        Зарегистрироваться
      </DXButton>
    </template>

    <template #error>
      <DXAlert v-if="error" type="error" :message="error" />
    </template>
  </DXAuthenticationForm>
</template>

<script setup>
import { ref } from "vue";
import {
  DXAuthenticationForm,
  DXFormControl,
  DXInput,
  DXPasswordInput,
  DXButton,
  DXCheckbox,
  DXLink,
  DXAlert,
} from "dxd-style-code";

const authMode = ref("login");
const isLoading = ref(false);
const error = ref(null);

const loginForm = ref({
  email: "",
  password: "",
  remember: false,
});

const registerForm = ref({
  name: "",
  email: "",
  password: "",
  confirmPassword: "",
});

const handleSubmit = async (formData) => {
  isLoading.value = true;
  error.value = null;
  try {
    // Логика аутентификации
    console.log("Отправка формы:", formData);
  } catch (err) {
    error.value = err.message;
  } finally {
    isLoading.value = false;
  }
};

const handleModeChange = (mode) => {
  authMode.value = mode;
};
</script>
```

---

### DXUserProfileCard

**Приоритет**: Низкий

**Назначение**: Карточка профиля пользователя с информацией, действиями и настройками.

**Сценарии использования**:

- Профили пользователей
- Карточки в списках пользователей
- Виджеты профиля
- Панели управления профилем

**Ключевые функции**:

- Отображение аватара и основной информации
- Контактная информация
- Статистика и метрики
- Действия (редактирование, отправка сообщения)
- Социальные связи
- Настройки приватности
- Поддержка различных размеров и стилей

**Интеграция**:

- Использует `DXAvatar` для аватара
- Использует `DXButton` для действий
- Использует `DXBadge` для статусов
- Может использоваться в `DXCard`

**Пример использования**:

```vue
<template>
  <DXUserProfileCard
    :user="user"
    :show-actions="true"
    @edit="handleEdit"
    @message="handleMessage"
  >
    <template #avatar>
      <DXAvatar :src="user.avatar" :name="user.name" size="xl" />
    </template>

    <template #info>
      <h3>{{ user.name }}</h3>
      <p class="role">{{ user.role }}</p>
      <p class="bio">{{ user.bio }}</p>
    </template>

    <template #stats>
      <div class="stats">
        <div class="stat-item">
          <strong>{{ user.stats.posts }}</strong>
          <span>Постов</span>
        </div>
        <div class="stat-item">
          <strong>{{ user.stats.followers }}</strong>
          <span>Подписчиков</span>
        </div>
        <div class="stat-item">
          <strong>{{ user.stats.following }}</strong>
          <span>Подписок</span>
        </div>
      </div>
    </template>

    <template #actions>
      <DXButton variant="primary" @click="handleMessage"> Написать </DXButton>
      <DXButton variant="ghost" @click="handleEdit"> Редактировать </DXButton>
    </template>

    <template #badges>
      <DXBadge variant="success">Верифицирован</DXBadge>
      <DXBadge variant="info">Премиум</DXBadge>
    </template>
  </DXUserProfileCard>
</template>

<script setup>
import { ref } from "vue";
import { DXUserProfileCard, DXAvatar, DXButton, DXBadge } from "dxd-style-code";

const user = ref({
  id: 1,
  name: "John Doe",
  role: "Senior Developer",
  bio: "Passionate about web development and design",
  avatar: "/avatar.jpg",
  stats: {
    posts: 125,
    followers: 1250,
    following: 340,
  },
});

const handleEdit = () => {
  console.log("Редактирование профиля");
};

const handleMessage = () => {
  console.log("Отправка сообщения");
};
</script>
```

---

### DXSettingsPanel

**Приоритет**: Низкий

**Назначение**: Панель настроек с категориями, группами настроек и сохранением изменений.

**Сценарии использования**:

- Настройки приложения
- Настройки профиля пользователя
- Конфигурация системы
- Панели администратора

**Ключевые функции**:

- Категории настроек (табы или сайдбар)
- Группы настроек внутри категорий
- Различные типы полей (переключатели, селекты, инпуты)
- Валидация настроек
- Сохранение и отмена изменений
- Поиск по настройкам
- Экспорт/импорт настроек
- Сброс к значениям по умолчанию

**Интеграция**:

- Использует `DXTabs` для категорий
- Использует различные компоненты форм (`DXInput`, `DXSelect`, `DXToggle`)
- Использует `DXFormControl` для валидации
- Использует `DXButton` для сохранения

**Пример использования**:

```vue
<template>
  <DXSettingsPanel
    v-model:settings="settings"
    :categories="categories"
    @save="handleSave"
    @reset="handleReset"
  >
    <template #category-general>
      <DXFormControl label="Язык">
        <DXSelect v-model="settings.language" :options="languages" />
      </DXFormControl>
      <DXFormControl label="Часовой пояс">
        <DXSelect v-model="settings.timezone" :options="timezones" />
      </DXFormControl>
    </template>

    <template #category-notifications>
      <DXFormControl label="Email уведомления">
        <DXToggle v-model="settings.notifications.email" />
      </DXFormControl>
      <DXFormControl label="Push уведомления">
        <DXToggle v-model="settings.notifications.push" />
      </DXFormControl>
      <DXFormControl label="SMS уведомления">
        <DXToggle v-model="settings.notifications.sms" />
      </DXFormControl>
    </template>

    <template #category-privacy>
      <DXFormControl label="Публичный профиль">
        <DXToggle v-model="settings.privacy.public" />
      </DXFormControl>
      <DXFormControl label="Показывать email">
        <DXToggle v-model="settings.privacy.showEmail" />
      </DXFormControl>
    </template>

    <template #actions>
      <DXButton @click="handleReset"> Сбросить </DXButton>
      <DXButton variant="primary" @click="handleSave"> Сохранить </DXButton>
    </template>
  </DXSettingsPanel>
</template>

<script setup>
import { ref } from "vue";
import {
  DXSettingsPanel,
  DXFormControl,
  DXSelect,
  DXToggle,
  DXButton,
} from "dxd-style-code";

const categories = ref([
  { id: "general", label: "Общие" },
  { id: "notifications", label: "Уведомления" },
  { id: "privacy", label: "Приватность" },
]);

const settings = ref({
  language: "ru",
  timezone: "Europe/Moscow",
  notifications: {
    email: true,
    push: false,
    sms: false,
  },
  privacy: {
    public: true,
    showEmail: false,
  },
});

const languages = ref([
  { value: "ru", label: "Русский" },
  { value: "en", label: "English" },
]);

const timezones = ref([
  { value: "Europe/Moscow", label: "Москва (UTC+3)" },
  { value: "Europe/London", label: "Лондон (UTC+0)" },
]);

const handleSave = () => {
  console.log("Сохранение настроек:", settings.value);
};

const handleReset = () => {
  // Сброс к значениям по умолчанию
  console.log("Сброс настроек");
};
</script>
```

---

### DXWizard

**Приоритет**: Низкий

**Назначение**: Мастер настройки с шагами, валидацией и возможностью сохранения прогресса.

**Сценарии использования**:

- Первоначальная настройка приложения
- Мастер импорта данных
- Пошаговая настройка сложных процессов
- Онбординг новых пользователей

**Ключевые функции**:

- Навигация по шагам
- Индикатор прогресса
- Валидация на каждом шаге
- Сохранение прогресса
- Возможность вернуться к предыдущим шагам
- Пропуск опциональных шагов
- Подтверждение перед выходом
- Анимации переходов

**Интеграция**:

- Использует `DXFormWizard` как базовую функциональность
- Использует различные компоненты форм
- Использует `DXProgress` для индикатора

**Пример использования**:

```vue
<template>
  <DXWizard
    :steps="wizardSteps"
    :current-step="currentStep"
    @step-change="handleStepChange"
    @complete="handleComplete"
    @cancel="handleCancel"
  >
    <template #step-1>
      <h3>Добро пожаловать!</h3>
      <p>Давайте настроим ваше приложение</p>
    </template>

    <template #step-2>
      <DXFormControl label="Название проекта">
        <DXInput v-model="wizardData.projectName" />
      </DXFormControl>
      <DXFormControl label="Описание">
        <DXTextarea v-model="wizardData.description" />
      </DXFormControl>
    </template>

    <template #step-3>
      <DXFormControl label="Выберите шаблон">
        <DXSelect v-model="wizardData.template" :options="templates" />
      </DXFormControl>
    </template>

    <template #step-4>
      <h3>Готово!</h3>
      <p>Ваш проект настроен и готов к использованию</p>
    </template>
  </DXWizard>
</template>

<script setup>
import { ref } from "vue";
import {
  DXWizard,
  DXFormControl,
  DXInput,
  DXTextarea,
  DXSelect,
} from "dxd-style-code";

const currentStep = ref(0);

const wizardSteps = ref([
  { id: 1, title: "Приветствие" },
  { id: 2, title: "Основная информация" },
  { id: 3, title: "Шаблон" },
  { id: 4, title: "Завершение" },
]);

const wizardData = ref({
  projectName: "",
  description: "",
  template: "",
});

const templates = ref([
  { value: "blank", label: "Пустой проект" },
  { value: "blog", label: "Блог" },
  { value: "ecommerce", label: "Интернет-магазин" },
]);

const handleStepChange = (step) => {
  currentStep.value = step;
};

const handleComplete = () => {
  console.log("Мастер завершен:", wizardData.value);
};

const handleCancel = () => {
  console.log("Мастер отменен");
};
</script>
```

---

### DXDashboardWidget

**Приоритет**: Низкий

**Назначение**: Виджет для дашборда с настройками, обновлением данных и различными типами контента.

**Сценарии использования**:

- Виджеты на дашборде
- Настраиваемые панели мониторинга
- Виджеты с различными типами данных (графики, таблицы, статистика)

**Ключевые функции**:

- Различные типы виджетов (статистика, график, таблица, текст)
- Настройки виджета
- Обновление данных (автоматическое/ручное)
- Изменение размера виджета
- Удаление виджета
- Заголовок и описание виджета
- Действия виджета (кнопки, ссылки)

**Интеграция**:

- Используется в `DXDashboardGrid` для размещения
- Использует `DXStatCard` для статистики
- Использует `DXChartContainer` для графиков
- Использует `DXCard` как базовый контейнер

**Пример использования**:

```vue
<template>
  <DXDashboardWidget
    :id="widget.id"
    :type="widget.type"
    :title="widget.title"
    :config="widget.config"
    :editable="true"
    @edit="handleEdit"
    @delete="handleDelete"
    @refresh="handleRefresh"
  >
    <template #header>
      <div class="widget-header">
        <h4>{{ widget.title }}</h4>
        <div class="widget-actions">
          <DXButton
            size="sm"
            variant="ghost"
            icon="refresh"
            @click="handleRefresh"
          />
          <DXButton
            size="sm"
            variant="ghost"
            icon="settings"
            @click="handleEdit"
          />
          <DXButton
            size="sm"
            variant="ghost"
            icon="close"
            @click="handleDelete"
          />
        </div>
      </div>
    </template>

    <template #content>
      <DXStatCard
        v-if="widget.type === 'stat'"
        :title="widget.config.title"
        :value="widget.config.value"
        :icon="widget.config.icon"
      />
      <DXChartContainer
        v-else-if="widget.type === 'chart'"
        :type="widget.config.chartType"
        :data="widget.config.data"
      />
    </template>
  </DXDashboardWidget>
</template>

<script setup>
import { ref } from "vue";
import {
  DXDashboardWidget,
  DXStatCard,
  DXChartContainer,
  DXButton,
} from "dxd-style-code";

const widget = ref({
  id: 1,
  type: "stat",
  title: "Пользователи",
  config: {
    title: "Всего",
    value: 1250,
    icon: "users",
  },
});

const handleEdit = () => {
  console.log("Редактирование виджета");
};

const handleDelete = () => {
  console.log("Удаление виджета");
};

const handleRefresh = () => {
  console.log("Обновление данных виджета");
};
</script>
```

---

### DXDataFilter

**Приоритет**: Низкий

**Назначение**: Комплексный фильтр данных с различными типами условий, группировкой и сохранением фильтров.

**Сценарии использования**:

- Расширенная фильтрация в таблицах
- Фильтры в каталогах товаров
- Поиск с множественными критериями
- Аналитические панели с фильтрами

**Ключевые функции**:

- Различные типы фильтров (текст, число, дата, выбор, диапазон)
- Группировка условий (AND/OR)
- Сохранение фильтров
- Применение и сброс фильтров
- Предустановленные фильтры
- Экспорт/импорт фильтров
- Визуальное отображение активных фильтров

**Интеграция**:

- Используется в `DXTable` для фильтрации данных
- Использует `DXFilterGroup` для групп фильтров
- Использует различные компоненты форм для условий
- Использует `DXButton` для действий

**Пример использования**:

```vue
<template>
  <DXDataFilter
    v-model:filters="activeFilters"
    :filter-definitions="filterDefinitions"
    @apply="handleApply"
    @reset="handleReset"
    @save="handleSave"
  >
    <template #filter-text="{ filter }">
      <DXInput v-model="filter.value" :placeholder="filter.placeholder" />
    </template>

    <template #filter-select="{ filter }">
      <DXSelect v-model="filter.value" :options="filter.options" />
    </template>

    <template #filter-date="{ filter }">
      <DXDatePicker v-model="filter.value" :range="filter.range" />
    </template>

    <template #filter-number="{ filter }">
      <div class="number-range">
        <DXInput v-model="filter.value.min" type="number" placeholder="От" />
        <DXInput v-model="filter.value.max" type="number" placeholder="До" />
      </div>
    </template>

    <template #actions>
      <DXButton @click="handleReset"> Сбросить </DXButton>
      <DXButton variant="primary" @click="handleApply"> Применить </DXButton>
    </template>
  </DXDataFilter>
</template>

<script setup>
import { ref } from "vue";
import {
  DXDataFilter,
  DXInput,
  DXSelect,
  DXDatePicker,
  DXButton,
} from "dxd-style-code";

const activeFilters = ref({
  name: { value: "", type: "text" },
  status: { value: "", type: "select" },
  date: { value: null, type: "date", range: true },
  price: { value: { min: "", max: "" }, type: "number" },
});

const filterDefinitions = ref([
  {
    id: "name",
    label: "Название",
    type: "text",
    placeholder: "Введите название",
  },
  {
    id: "status",
    label: "Статус",
    type: "select",
    options: [
      { value: "active", label: "Активный" },
      { value: "inactive", label: "Неактивный" },
    ],
  },
  {
    id: "date",
    label: "Дата",
    type: "date",
    range: true,
  },
  {
    id: "price",
    label: "Цена",
    type: "number",
  },
]);

const handleApply = () => {
  console.log("Применение фильтров:", activeFilters.value);
};

const handleReset = () => {
  activeFilters.value = {
    name: { value: "", type: "text" },
    status: { value: "", type: "select" },
    date: { value: null, type: "date", range: true },
    price: { value: { min: "", max: "" }, type: "number" },
  };
};

const handleSave = () => {
  console.log("Сохранение фильтров");
};
</script>
```

---

### DXReportGenerator

**Приоритет**: Низкий

**Назначение**: Генератор отчетов с настройками параметров, форматов экспорта и предпросмотром.

**Сценарии использования**:

- Генерация отчетов в бизнес-приложениях
- Экспорт данных в различных форматах
- Настраиваемые отчеты
- Планирование автоматической генерации отчетов

**Ключевые функции**:

- Выбор параметров отчета
- Выбор формата экспорта (PDF, Excel, CSV, JSON)
- Предпросмотр отчета
- Настройка полей и группировки
- Фильтрация данных для отчета
- Сохранение шаблонов отчетов
- Планирование генерации отчетов
- Отправка отчетов по email

**Интеграция**:

- Использует `DXDataFilter` для фильтрации данных
- Использует `DXTable` для предпросмотра
- Использует `DXButton` для генерации и экспорта
- Использует `DXModal` для настроек

**Пример использования**:

```vue
<template>
  <DXReportGenerator
    :data="reportData"
    :columns="reportColumns"
    @generate="handleGenerate"
    @export="handleExport"
  >
    <template #filters>
      <DXDataFilter
        v-model:filters="reportFilters"
        :filter-definitions="filterDefinitions"
      />
    </template>

    <template #preview>
      <DXTable
        :columns="reportColumns"
        :data="filteredData"
        :pagination="false"
      />
    </template>

    <template #settings>
      <DXFormControl label="Формат экспорта">
        <DXSelect v-model="exportFormat" :options="exportFormats" />
      </DXFormControl>
      <DXFormControl label="Включить графики">
        <DXToggle v-model="includeCharts" />
      </DXFormControl>
      <DXFormControl label="Группировка">
        <DXSelect v-model="groupBy" :options="groupByOptions" />
      </DXFormControl>
    </template>

    <template #actions>
      <DXButton @click="handlePreview"> Предпросмотр </DXButton>
      <DXButton variant="primary" @click="handleGenerate">
        Сгенерировать
      </DXButton>
      <DXButton variant="primary" @click="handleExport">
        Экспортировать
      </DXButton>
    </template>
  </DXReportGenerator>
</template>

<script setup>
import { ref, computed } from "vue";
import {
  DXReportGenerator,
  DXDataFilter,
  DXTable,
  DXFormControl,
  DXSelect,
  DXToggle,
  DXButton,
} from "dxd-style-code";

const reportData = ref([
  { id: 1, name: "Товар 1", price: 100, date: "2024-01-15" },
  { id: 2, name: "Товар 2", price: 200, date: "2024-01-20" },
]);

const reportColumns = ref([
  { key: "name", label: "Название" },
  { key: "price", label: "Цена" },
  { key: "date", label: "Дата" },
]);

const reportFilters = ref({});
const exportFormat = ref("pdf");
const includeCharts = ref(false);
const groupBy = ref("");

const exportFormats = ref([
  { value: "pdf", label: "PDF" },
  { value: "excel", label: "Excel" },
  { value: "csv", label: "CSV" },
]);

const groupByOptions = ref([
  { value: "", label: "Без группировки" },
  { value: "date", label: "По дате" },
  { value: "category", label: "По категории" },
]);

const filterDefinitions = ref([
  { id: "date", label: "Дата", type: "date", range: true },
]);

const filteredData = computed(() => {
  // Логика фильтрации
  return reportData.value;
});

const handleGenerate = () => {
  console.log("Генерация отчета:", {
    format: exportFormat.value,
    includeCharts: includeCharts.value,
    groupBy: groupBy.value,
  });
};

const handleExport = () => {
  console.log("Экспорт отчета:", exportFormat.value);
};

const handlePreview = () => {
  console.log("Предпросмотр отчета");
};
</script>
```

---

## 🔧 Утилитарные компоненты

### DXPortal

**Приоритет**: Высокий

**Назначение**: Компонент для рендеринга контента вне текущей DOM-иерархии (в body или другой контейнер).

**Сценарии использования**:

- Модальные окна (рендер в body)
- Тулы и подсказки (рендер в body)
- Выпадающие меню (рендер в body)
- Уведомления и тосты (рендер в body)
- Любые компоненты, которые должны быть поверх всего контента

**Ключевые функции**:

- Рендеринг в указанный контейнер (по умолчанию body)
- Автоматическая очистка при размонтировании
- Поддержка нескольких порталов
- Управление z-index
- Сохранение контекста Vue (provide/inject)

**Интеграция**:

- Используется в `DXModal` для рендеринга модальных окон
- Используется в `DXTooltip` для подсказок
- Используется в `DXDropdown` для выпадающих меню
- Используется в `DXToast` для уведомлений

**Пример использования**:

```vue
<template>
  <div>
    <DXButton @click="showModal = true"> Открыть модальное окно </DXButton>

    <!-- Модальное окно через Portal -->
    <DXPortal v-if="showModal" to="body">
      <DXModal @close="showModal = false">
        <h2>Модальное окно</h2>
        <p>Это содержимое рендерится в body через Portal</p>
      </DXModal>
    </DXPortal>

    <!-- Tooltip через Portal -->
    <DXPortal to="body">
      <DXTooltip :target="tooltipTarget" :show="showTooltip">
        Подсказка, рендерится в body
      </DXTooltip>
    </DXPortal>

    <!-- Кастомный контейнер -->
    <div id="custom-portal"></div>
    <DXPortal to="#custom-portal">
      <div>Контент в кастомном контейнере</div>
    </DXPortal>
  </div>
</template>

<script setup>
import { ref } from "vue";
import { DXPortal, DXModal, DXTooltip, DXButton } from "dxd-style-code";

const showModal = ref(false);
const showTooltip = ref(false);
const tooltipTarget = ref(null);
</script>
```

---

### DXObserver

**Приоритет**: Средний

**Назначение**: Компонент для отслеживания видимости элементов с использованием Intersection Observer API.

**Сценарии использования**:

- Ленивая загрузка изображений
- Бесконечная прокрутка
- Анимации при появлении в viewport
- Отслеживание видимости для аналитики
- Подгрузка контента при прокрутке

**Ключевые функции**:

- Отслеживание видимости элемента
- Callback при появлении/исчезновении
- Настройка порога видимости
- Поддержка root margin
- Отслеживание нескольких элементов
- Оптимизация производительности

**Интеграция**:

- Используется в `DXMediaGallery` для ленивой загрузки
- Используется в `DXTable` для бесконечной прокрутки
- Может использоваться в любых компонентах для анимаций

**Пример использования**:

```vue
<template>
  <div>
    <!-- Ленивая загрузка изображений -->
    <DXObserver :threshold="0.1" @intersect="handleIntersect">
      <img v-if="isVisible" :src="imageSrc" alt="Lazy loaded image" />
      <div v-else class="placeholder">Загрузка...</div>
    </DXObserver>

    <!-- Бесконечная прокрутка -->
    <div class="scroll-container" ref="scrollContainer">
      <div v-for="item in items" :key="item.id" class="item">
        {{ item.content }}
      </div>
      <DXObserver :threshold="0.5" @intersect="loadMore">
        <div v-if="isLoading">Загрузка...</div>
      </DXObserver>
    </div>

    <!-- Анимация при появлении -->
    <DXObserver :threshold="0.3" @intersect="handleAnimate">
      <div class="animated-element" :class="{ 'is-visible': isAnimated }">
        Элемент появляется при скролле
      </div>
    </DXObserver>
  </div>
</template>

<script setup>
import { ref } from "vue";
import { DXObserver } from "dxd-style-code";

const isVisible = ref(false);
const isLoading = ref(false);
const isAnimated = ref(false);
const imageSrc = ref("/image.jpg");
const scrollContainer = ref(null);

const items = ref([
  { id: 1, content: "Элемент 1" },
  { id: 2, content: "Элемент 2" },
]);

const handleIntersect = (isIntersecting) => {
  if (isIntersecting && !isVisible.value) {
    isVisible.value = true;
  }
};

const loadMore = (isIntersecting) => {
  if (isIntersecting && !isLoading.value) {
    isLoading.value = true;
    // Загрузка дополнительных данных
    setTimeout(() => {
      items.value.push({
        id: items.value.length + 1,
        content: `Элемент ${items.value.length + 1}`,
      });
      isLoading.value = false;
    }, 1000);
  }
};

const handleAnimate = (isIntersecting) => {
  if (isIntersecting) {
    isAnimated.value = true;
  }
};
</script>

<style scoped>
.animated-element {
  opacity: 0;
  transform: translateY(20px);
  transition: all 0.5s;
}

.animated-element.is-visible {
  opacity: 1;
  transform: translateY(0);
}
</style>
```

---

### DXThemeProvider

**Приоритет**: Средний

**Назначение**: Провайдер для управления темами и цветовыми схемами приложения.

**Сценарии использования**:

- Переключение между светлой и темной темой
- Кастомные цветовые схемы
- Динамическое изменение темы
- Сохранение выбранной темы

**Ключевые функции**:

- Управление текущей темой
- Переключение между темами
- Кастомные цветовые схемы
- Применение CSS переменных для темы
- Сохранение темы в localStorage
- Поддержка системной темы
- Provide/inject для доступа к теме в компонентах

**Интеграция**:

- Используется на уровне приложения
- Все компоненты получают доступ к теме через provide/inject
- Интегрируется с CSS переменными для стилизации

**Пример использования**:

```vue
<template>
  <DXThemeProvider
    :theme="currentTheme"
    :themes="availableThemes"
    @theme-change="handleThemeChange"
  >
    <div class="app">
      <header>
        <h1>Мое приложение</h1>
        <DXButton @click="toggleTheme"> Переключить тему </DXButton>
      </header>

      <main>
        <DXCard>
          <h2>Контент</h2>
          <p>Этот контент использует текущую тему</p>
        </DXCard>
      </main>
    </div>
  </DXThemeProvider>
</template>

<script setup>
import { ref, provide } from "vue";
import { DXThemeProvider, DXCard, DXButton } from "dxd-style-code";

const currentTheme = ref("light");

const availableThemes = ref({
  light: {
    name: "Светлая",
    colors: {
      primary: "#007bff",
      background: "#ffffff",
      text: "#000000",
    },
  },
  dark: {
    name: "Темная",
    colors: {
      primary: "#0d6efd",
      background: "#1a1a1a",
      text: "#ffffff",
    },
  },
});

const toggleTheme = () => {
  currentTheme.value = currentTheme.value === "light" ? "dark" : "light";
};

const handleThemeChange = (theme) => {
  currentTheme.value = theme;
  // Сохранение в localStorage
  localStorage.setItem("theme", theme);
};

// В компонентах можно использовать тему через inject
// const theme = inject('theme')
</script>

<style>
:root {
  --color-primary: v-bind("availableThemes[currentTheme].colors.primary");
  --color-background: v-bind("availableThemes[currentTheme].colors.background");
  --color-text: v-bind("availableThemes[currentTheme].colors.text");
}
</style>
```

---

### DXBreakpointProvider

**Приоритет**: Низкий

**Назначение**: Провайдер для доступа к текущему брейкпоинту и размерам экрана в компонентах.

**Сценарии использования**:

- Адаптивное поведение компонентов
- Условный рендеринг на основе размера экрана
- Программный доступ к брейкпоинтам
- Адаптивная логика в компонентах

**Ключевые функции**:

- Отслеживание размера окна
- Определение текущего брейкпоинта
- Реактивные значения для использования в компонентах
- Поддержка кастомных брейкпоинтов
- Оптимизация производительности (debounce)

**Интеграция**:

- Используется на уровне приложения
- Компоненты получают доступ через provide/inject
- Используется в адаптивных компонентах (`DXSidebar`, `DXHeaderBar`)

**Пример использования**:

```vue
<template>
  <DXBreakpointProvider>
    <div class="app">
      <!-- Адаптивный контент -->
      <div v-if="isMobile" class="mobile-layout">
        <DXButton icon="menu" @click="toggleMenu" />
      </div>

      <div v-else class="desktop-layout">
        <DXSidebar :collapsed="false" />
      </div>

      <!-- Условный рендеринг на основе брейкпоинта -->
      <DXCard>
        <h2>Текущий брейкпоинт: {{ currentBreakpoint }}</h2>
        <p>Ширина экрана: {{ screenWidth }}px</p>
        <p>Высота экрана: {{ screenHeight }}px</p>
      </DXCard>

      <!-- Адаптивная сетка -->
      <DXGrid :columns="gridColumns" :gap="gridGap">
        <div v-for="i in 6" :key="i" class="grid-item">Элемент {{ i }}</div>
      </DXGrid>
    </div>
  </DXBreakpointProvider>
</template>

<script setup>
import { ref, computed, inject } from "vue";
import {
  DXBreakpointProvider,
  DXCard,
  DXButton,
  DXSidebar,
  DXGrid,
} from "dxd-style-code";

// Получение брейкпоинтов через inject
const breakpoints = inject("breakpoints");
const currentBreakpoint = inject("currentBreakpoint");
const screenWidth = inject("screenWidth");
const screenHeight = inject("screenHeight");

const isMobile = computed(
  () => currentBreakpoint.value === "sm" || currentBreakpoint.value === "xs"
);

const gridColumns = computed(() => {
  switch (currentBreakpoint.value) {
    case "xs":
      return 1;
    case "sm":
      return 2;
    case "md":
      return 3;
    case "lg":
      return 4;
    default:
      return 4;
  }
});

const gridGap = computed(() => {
  return isMobile.value ? "1rem" : "2rem";
});

const toggleMenu = () => {
  // Логика мобильного меню
};
</script>
```

---

## 🎬 Анимационные компоненты

### DXTransitionGroup

**Приоритет**: Средний

**Назначение**: Компонент для групповых анимаций списков элементов с поддержкой различных типов переходов.

**Сценарии использования**:

- Анимации списков при добавлении/удалении элементов
- Анимации карточек в сетке
- Анимации элементов в таблице
- Любые коллекции элементов с анимациями

**Ключевые функции**:

- Групповые анимации для списков
- Поддержка различных типов переходов (fade, slide, scale)
- Анимации при добавлении/удалении элементов
- Настройка длительности и easing
- Поддержка key для правильной анимации элементов
- Оптимизация производительности

**Интеграция**:

- Используется в `DXList` для анимации элементов
- Используется в `DXTable` для анимации строк
- Может использоваться в любых компонентах со списками

**Пример использования**:

```vue
<template>
  <div>
    <!-- Анимация списка элементов -->
    <DXTransitionGroup name="list" tag="ul" class="item-list">
      <li v-for="item in items" :key="item.id" class="list-item">
        {{ item.name }}
        <DXButton size="sm" @click="removeItem(item.id)"> Удалить </DXButton>
      </li>
    </DXTransitionGroup>

    <DXButton @click="addItem"> Добавить элемент </DXButton>

    <!-- Анимация карточек в сетке -->
    <DXTransitionGroup name="grid" tag="div" class="card-grid">
      <DXCard v-for="card in cards" :key="card.id" class="grid-card">
        <h3>{{ card.title }}</h3>
        <p>{{ card.content }}</p>
      </DXCard>
    </DXTransitionGroup>
  </div>
</template>

<script setup>
import { ref } from "vue";
import { DXTransitionGroup, DXCard, DXButton } from "dxd-style-code";

const items = ref([
  { id: 1, name: "Элемент 1" },
  { id: 2, name: "Элемент 2" },
  { id: 3, name: "Элемент 3" },
]);

const cards = ref([
  { id: 1, title: "Карточка 1", content: "Содержимое 1" },
  { id: 2, title: "Карточка 2", content: "Содержимое 2" },
]);

const addItem = () => {
  const newId = Math.max(...items.value.map((i) => i.id)) + 1;
  items.value.push({ id: newId, name: `Элемент ${newId}` });
};

const removeItem = (id) => {
  items.value = items.value.filter((item) => item.id !== id);
};
</script>

<style scoped>
.list-enter-active,
.list-leave-active {
  transition: all 0.3s;
}

.list-enter-from {
  opacity: 0;
  transform: translateX(-30px);
}

.list-leave-to {
  opacity: 0;
  transform: translateX(30px);
}

.grid-enter-active,
.grid-leave-active {
  transition: all 0.3s;
}

.grid-enter-from {
  opacity: 0;
  transform: scale(0.8);
}

.grid-leave-to {
  opacity: 0;
  transform: scale(0.8);
}
</style>
```

---

### DXAnimatePresence

**Приоритет**: Средний

**Назначение**: Компонент для анимаций появления и исчезания элементов с поддержкой условного рендеринга.

**Сценарии использования**:

- Анимации модальных окон
- Анимации выпадающих меню
- Анимации уведомлений
- Анимации при переключении контента

**Ключевые функции**:

- Анимации появления/исчезания
- Поддержка различных типов переходов
- Условный рендеринг с анимацией
- Настройка длительности и easing
- Callback при завершении анимации
- Поддержка начального состояния

**Интеграция**:

- Используется в `DXModal` для анимации открытия/закрытия
- Используется в `DXDropdown` для анимации меню
- Используется в `DXToast` для анимации уведомлений
- Используется в `DXTabs` для анимации контента

**Пример использования**:

```vue
<template>
  <div>
    <!-- Анимация модального окна -->
    <DXButton @click="showModal = !showModal">
      {{ showModal ? "Закрыть" : "Открыть" }} модальное окно
    </DXButton>

    <DXAnimatePresence>
      <DXModal
        v-if="showModal"
        @close="showModal = false"
        transition="fade-scale"
      >
        <h2>Модальное окно</h2>
        <p>С анимацией появления и исчезания</p>
      </DXModal>
    </DXAnimatePresence>

    <!-- Анимация выпадающего меню -->
    <DXButton @click="showDropdown = !showDropdown"> Меню </DXButton>

    <DXAnimatePresence>
      <DXDropdown
        v-if="showDropdown"
        :items="menuItems"
        transition="slide-down"
      />
    </DXAnimatePresence>

    <!-- Анимация уведомлений -->
    <DXButton @click="addNotification"> Показать уведомление </DXButton>

    <div class="notifications">
      <DXAnimatePresence>
        <DXToast
          v-for="notification in notifications"
          :key="notification.id"
          :type="notification.type"
          :message="notification.message"
          transition="slide-right"
          @close="removeNotification(notification.id)"
        />
      </DXAnimatePresence>
    </div>

    <!-- Анимация переключения контента -->
    <DXTabs v-model:active="activeTab" :tabs="tabs">
      <DXAnimatePresence mode="out-in">
        <div :key="activeTab" transition="fade">
          <h3>{{ tabs.find((t) => t.id === activeTab)?.label }}</h3>
          <p>Контент вкладки {{ activeTab }}</p>
        </div>
      </DXAnimatePresence>
    </DXTabs>
  </div>
</template>

<script setup>
import { ref } from "vue";
import {
  DXAnimatePresence,
  DXModal,
  DXDropdown,
  DXToast,
  DXTabs,
  DXButton,
} from "dxd-style-code";

const showModal = ref(false);
const showDropdown = ref(false);
const activeTab = ref(1);

const menuItems = ref([
  { id: 1, label: "Пункт 1" },
  { id: 2, label: "Пункт 2" },
]);

const notifications = ref([]);

const tabs = ref([
  { id: 1, label: "Вкладка 1" },
  { id: 2, label: "Вкладка 2" },
  { id: 3, label: "Вкладка 3" },
]);

const addNotification = () => {
  const id = Date.now();
  notifications.value.push({
    id,
    type: "success",
    message: "Уведомление добавлено!",
  });

  setTimeout(() => {
    removeNotification(id);
  }, 3000);
};

const removeNotification = (id) => {
  notifications.value = notifications.value.filter((n) => n.id !== id);
};
</script>
```

---

### DXStaggeredAnimation

**Приоритет**: Низкий

**Назначение**: Компонент для последовательных анимаций элементов с задержкой между ними.

**Сценарии использования**:

- Анимации списков с последовательным появлением
- Анимации карточек в сетке
- Эффектные анимации загрузки контента
- Анимации элементов дашборда

**Ключевые функции**:

- Последовательные анимации с задержкой
- Настройка задержки между элементами
- Поддержка различных типов анимаций
- Направление анимации (сверху вниз, слева направо)
- Настройка длительности и easing
- Оптимизация производительности

**Интеграция**:

- Используется в `DXDashboardGrid` для анимации виджетов
- Используется в `DXList` для эффектного появления элементов
- Может использоваться в любых компонентах со списками

**Пример использования**:

```vue
<template>
  <div>
    <!-- Последовательная анимация списка -->
    <DXStaggeredAnimation
      :delay="100"
      direction="top-to-bottom"
      transition="fade-slide"
    >
      <div v-for="item in items" :key="item.id" class="animated-item">
        {{ item.name }}
      </div>
    </DXStaggeredAnimation>

    <!-- Анимация карточек в сетке -->
    <DXStaggeredAnimation
      :delay="150"
      direction="left-to-right"
      transition="scale"
    >
      <DXCard v-for="card in cards" :key="card.id" class="animated-card">
        <h3>{{ card.title }}</h3>
        <p>{{ card.content }}</p>
      </DXCard>
    </DXStaggeredAnimation>

    <!-- Анимация виджетов дашборда -->
    <DXDashboardGrid>
      <DXStaggeredAnimation
        :delay="200"
        direction="top-to-bottom"
        transition="fade-scale"
      >
        <DXDashboardWidget
          v-for="widget in widgets"
          :key="widget.id"
          :config="widget"
        />
      </DXStaggeredAnimation>
    </DXDashboardGrid>

    <DXButton @click="loadMore"> Загрузить еще </DXButton>
  </div>
</template>

<script setup>
import { ref } from "vue";
import {
  DXStaggeredAnimation,
  DXCard,
  DXDashboardGrid,
  DXDashboardWidget,
  DXButton,
} from "dxd-style-code";

const items = ref([
  { id: 1, name: "Элемент 1" },
  { id: 2, name: "Элемент 2" },
  { id: 3, name: "Элемент 3" },
]);

const cards = ref([
  { id: 1, title: "Карточка 1", content: "Содержимое 1" },
  { id: 2, title: "Карточка 2", content: "Содержимое 2" },
  { id: 3, title: "Карточка 3", content: "Содержимое 3" },
]);

const widgets = ref([
  { id: 1, type: "stat", title: "Виджет 1" },
  { id: 2, type: "chart", title: "Виджет 2" },
]);

const loadMore = () => {
  const newId = Math.max(...items.value.map((i) => i.id)) + 1;
  items.value.push({ id: newId, name: `Элемент ${newId}` });
};
</script>

<style scoped>
.animated-item {
  padding: 1rem;
  margin: 0.5rem 0;
  background: #f5f5f5;
  border-radius: 4px;
}

.animated-card {
  margin: 1rem;
}
</style>
```

---

## 📊 Приоритизация и фазы разработки

### Фаза 1: Критические компоненты (Высокий приоритет)

**Цель**: Закрыть базовые потребности в навигации, макете и утилитах.

**Компоненты**:

1. **DXLink** - базовый элемент навигации
2. **DXTimePicker** - часто используется вместе с DatePicker
3. **DXAppLayout** - базовая структура приложения
4. **DXHeaderBar** - стандартный элемент интерфейса
5. **DXPortal** - критически важен для модалок и тултипов

**Ожидаемый результат**: Базовый набор для создания полноценных приложений с правильной структурой и навигацией.

---

### Фаза 2: Важные компоненты (Средний приоритет)

**Цель**: Расширить функциональность форм, дашбордов и улучшить UX.

**Компоненты**:

1. **DXSearchBar** - улучшение поиска
2. **DXStatCard** - для дашбордов
3. **DXRating** - популярный компонент
4. **DXSidebar** - обертка для боковой панели
5. **DXDashboardGrid** - для настраиваемых дашбордов
6. **DXFormWizard** - многошаговые формы
7. **DXMediaGallery** - часто требуется
8. **DXObserver** - для оптимизации производительности
9. **DXThemeProvider** - управление темами
10. **DXTransitionGroup** - групповые анимации
11. **DXAnimatePresence** - анимации появления

**Ожидаемый результат**: Расширенный набор компонентов для создания сложных интерфейсов с улучшенным UX.

---

### Фаза 3: Специализированные компоненты (Низкий приоритет)

**Цель**: Добавить специализированные компоненты для конкретных сценариев использования.

**Компоненты**:

1. **DXQuote** - inline цитаты
2. **DXDataTable** - расширенная таблица
3. **DXChartContainer** - контейнер графиков
4. **DXCommentSection** - секция комментариев
5. **DXNotificationCenter** - центр уведомлений
6. **DXChatInterface** - интерфейс чата
7. **DXAuthenticationForm** - форма аутентификации
8. **DXUserProfileCard** - карточка профиля
9. **DXSettingsPanel** - панель настроек
10. **DXWizard** - мастер настройки
11. **DXDashboardWidget** - виджет дашборда
12. **DXDataFilter** - комплексный фильтр
13. **DXReportGenerator** - генератор отчетов
14. **DXBreakpointProvider** - провайдер брейкпоинтов
15. **DXStaggeredAnimation** - последовательные анимации

**Ожидаемый результат**: Полный набор компонентов для любых сценариев использования.

---

## 📝 Примечания

### Принципы разработки

При реализации каждого компонента необходимо учитывать:

1. **Семантическая разметка**: Все компоненты должны использовать правильные HTML-теги и ARIA-атрибуты
2. **Доступность (a11y)**: Поддержка навигации с клавиатуры, правильные роли и атрибуты
3. **Адаптивность**: Мобильный-first подход, поддержка различных размеров экранов
4. **Композиция**: Использование slots для кастомизации, props для настройки
5. **Производительность**: Оптимизация рендеринга, ленивая загрузка где возможно
6. **Типизация**: Полная типизация props и событий
7. **Документация**: Storybook stories для каждого компонента

### Интеграция с существующими компонентами

При добавлении новых компонентов важно:

- Использовать существующие атомы и молекулы где возможно
- Следовать существующим паттернам именования и структуры
- Использовать существующие composables и утилиты
- Поддерживать единый стиль и дизайн-систему
- Добавлять примеры использования в Storybook

### Расширяемость

Библиотека должна быть расширяемой:

- Возможность кастомизации через CSS переменные
- Поддержка тем оформления
- Возможность переопределения стилей
- Плагинная архитектура где возможно

---

## 🎯 Итоговая статистика

**Текущее состояние**: 70 компонентов

**Планируется добавить**: 32 компонента

**Итого после реализации**: 102 компонента

**Распределение по категориям**:

- Атомы: +2 компонента (25 всего)
- Молекулы: +4 компонента (31 всего)
- Организмы: +18 компонентов (25 всего)
- Утилитарные: +4 компонента (4 всего)
- Анимационные: +3 компонента (3 всего)
- Типография: без изменений (6 компонентов)
- Макет: без изменений (7 компонентов)

---

_Последнее обновление: 2024_
